## Environment setup

```python3 -m venv .venv```  
```source .venv/bin/activate```  

```pip install -r gym-custom-env-routing/requirements.txt```  

(Optional)  
```cd gym-custom-env-routing```  
```python myrun.py```  

From project/ directory:
```python test_env.py```  
